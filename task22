##Даны два неупорядоченных набора целых чисел (может быть, с повторениями). Выдать без повторений в порядке возрастания все те числа, которые встречаются в обоих наборах.
##Пользователь вводит 2 числа. n — кол-во элементов первого множества. m — кол-во элементов второго множества. Затем пользователь вводит сами элементы множеств.

def intersection_of_sets(n, m):
    # Вводим элементы первого множества и сохраняем их во множестве set1
    set1 = set(map(int, input("Введите элементы первого множества через пробел: ").split()))

    # Вводим элементы второго множества и сохраняем их во множестве set2
    set2 = set(map(int, input("Введите элементы второго множества через пробел: ").split()))

    # Находим пересечение множеств set1 и set2 и выводим его отсортированным в порядке возрастания
    intersection = sorted(list(set1.intersection(set2)))
    return intersection

# Вводим количество элементов в первом и втором множествах
n = int(input("Введите количество элементов в первом множестве: "))
m = int(input("Введите количество элементов во втором множестве: "))

# Находим пересечение множеств и выводим результат
result = intersection_of_sets(n, m)
print("Числа, которые встречаются в обоих множествах:", result)
